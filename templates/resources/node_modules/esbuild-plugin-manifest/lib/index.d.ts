import { Plugin } from 'esbuild';
declare type OptionValue = boolean | 'input' | 'output';
interface ManifestEntries {
    [key: string]: string;
}
interface ManifestPluginOptions {
    hash?: boolean;
    shortNames?: OptionValue;
    filename?: string;
    extensionless?: OptionValue;
    useEntrypointKeys?: boolean;
    append?: boolean;
    generate?: (entries: ManifestEntries) => Object;
    filter?: (filename: string) => boolean;
}
declare const _default: (options?: ManifestPluginOptions) => Plugin;
export = _default;
