import { h } from 'preact';
import { AddressLookupItem } from '../types';
import './AddressSearch.scss';
import { AddressData } from '../../../../types';
export type OnAddressLookupType = (value: string, actions: {
    resolve: (value: Array<AddressLookupItem>) => void;
    reject: (reason?: any) => void;
}) => Promise<void>;
export type OnAddressSelectedType = (value: string, actions: {
    resolve: (value: AddressLookupItem) => void;
    reject: (reason?: any) => void;
}) => Promise<void>;
interface AddressSearchProps {
    onAddressLookup?: OnAddressLookupType;
    onAddressSelected?: OnAddressSelectedType;
    onSelect: (addressItem: AddressData) => void;
    onManualAddress: any;
    externalErrorMessage: string;
    hideManualButton: boolean;
    addressSearchDebounceMs?: number;
}
export default function AddressSearch({ onAddressLookup, onAddressSelected, onSelect, onManualAddress, externalErrorMessage, hideManualButton, addressSearchDebounceMs }: Readonly<AddressSearchProps>): h.JSX.Element;
export {};
