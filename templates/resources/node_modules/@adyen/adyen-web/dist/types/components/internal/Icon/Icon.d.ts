import { h } from 'preact';
interface IconProps {
    type: string;
    className?: string;
    alt?: string;
    height?: number;
    width?: number;
}
declare const Icon: ({ type, className, alt, height, width }: IconProps) => h.JSX.Element;
export default Icon;
