import { h } from 'preact';
import './CtPInfoModal.scss';
type CtPInfoModalProps = {
    isOpen: boolean;
    onClose(): void;
    focusAfterClose: HTMLElement;
};
declare const CtPInfoModal: ({ isOpen, onClose, focusAfterClose }: CtPInfoModalProps) => h.JSX.Element;
export { CtPInfoModal };
