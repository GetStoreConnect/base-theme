import { h } from 'preact';
import './SegmentedControl.scss';
interface SegmentedControlProps<T> {
    classNameModifiers?: string[];
    selectedValue: T;
    disabled?: boolean;
    options: Array<{
        label: string;
        value: T;
        htmlProps?: any;
    }>;
    onChange(value: T, event: MouseEvent): void;
}
declare function SegmentedControl<T>({ classNameModifiers, selectedValue, disabled, options, onChange }: SegmentedControlProps<T>): h.JSX.Element;
export default SegmentedControl;
